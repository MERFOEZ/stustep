{
  "name": "stustep-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "اختبارات آلية لقواعد أمان Firestore على المحاكي المحلي",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --only firestore --project stustep-rules-test \"mocha firestore_rules_test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.35.1",
    "mocha": "^10.8.2"
  }
}
